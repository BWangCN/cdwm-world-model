# Core (training / eval / inference)
torch>=2.1
numpy>=1.24
# Eval metrics / analysis
scipy>=1.10
# Visualization (viz/): analyze_direction needs matplotlib; render_direction_video additionally needs mujoco + imageio
#   + our simulation harness (not included — see viz/README.md)
matplotlib>=3.7
imageio>=2.31
Pillow>=10.0
# mujoco>=3.0        # only for viz/render_direction_video.py
