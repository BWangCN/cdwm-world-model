# Core (training / eval / inference)
torch>=2.1
numpy>=1.24
scipy>=1.10
# Geometry + physics sim (drop data generation, stable poses, physical-density check)
trimesh>=4.0
mujoco==2.3.7          # pinned: the drop episodes regenerate deterministically only on this version
coacd>=1.0            # convex decomposition for the physical-density realism check
# Visualization / demos (render_drop_demo, render_basins)
imageio>=2.31
imageio-ffmpeg>=0.4
Pillow>=10.0
matplotlib>=3.7
